我先鎖定兩件事：確認目前 5.5 的文件落點與現有命令輸出格式，然後建立 regression 規格文件；之後才批次跑各個小窗口的 validate/write-plan，以及限定範圍的 actual write。

Read [](file:///Users/pork/sofone-project/IA-Analyses/ia-analyses-db/%E6%96%87%E4%BB%B6)

Read [](file:///Users/pork/sofone-project/IA-Analyses/ia-analyses-db/README.md#130-130), lines 130 to 230

Read [](file:///Users/pork/sofone-project/IA-Analyses/ia-analyses-db/%E6%96%87%E4%BB%B6/%E6%9E%B6%E6%A7%8B%E6%8C%87%E5%8D%97.md#80-80), lines 80 to 220

Read [](file:///Users/pork/sofone-project/IA-Analyses/ia-analyses-db/%E6%96%87%E4%BB%B6/fact_implementation_sequence.md#198-198), lines 198 to 270

Read [](file:///Users/pork/sofone-project/IA-Analyses/ia-analyses-db/%E6%96%87%E4%BB%B6/%E6%9B%B4%E6%96%B0%E7%B4%80%E9%8C%84.md#1-1), lines 1 to 40

先把 regression 的判準文件補齊，這樣後面的命令執行與失敗分類都有固定依據，不會邊跑邊變口徑。

Generating patch (277 lines) in [](file:///Users/pork/sofone-project/IA-Analyses/ia-analyses-db/%E6%96%87%E4%BB%B6/sales_fact_correctness_basis_phase2c.md), [](file:///Users/pork/sofone-project/IA-Analyses/ia-analyses-db/%E6%96%87%E4%BB%B6/sales_fact_regression_windows_phase2c.md)


```

```

```

```
correctness basis 與 regression window plan 文件已補好，這輪暫時沒有動 Go。下一步直接跑 regression 命令矩陣，先把所有 read-only window 的穩定性掃過一遍，再只對已開放範圍做 actual write。

我先跑你指定的 go build，接著會用一個受控腳本把 7 個小窗口的 validate-only 與 write-plan 全部掃完並抽出摘要，避免 14 份完整 Athena 輸出淹沒真正的結果。

Ran terminal command:  go build ./...

Ran terminal command:  cd /Users/pork/sofone-project/IA-Analyses/ia-analyses-db && tmpdir=$(mktemp -d) && windows=("w0101|2025-01-01|2025-01-01" "w0102|2025-01-02|2025-01-02" "w0101_0102|2025-01-01|2025-01-02" "w0107|2025-01-07|2025-01-07" "w0115|2025-01-15|2025-01-15" "w0131|2025-01-31|2025-01-31" "w0131_0201|2025-01-31|2025-02-01") && echo "tmpdir=$tmpdir" && for entry in "${windows[@]}"; do
  label=${entry%%|*}
  rest=${entry#*|}
  start=${rest%%|*}
  end=${rest#*|}
  for mode in validate writeplan; do
    outfile="$tmpdir/${label}_${mode}.txt"
    if [[ "$mode" == "validate" ]]; then
      make sync-athena-validate OWNER_USER_KEY=demo-owner OWNER_USER_ID=1 START_DATE="$start" END_DATE="$end" >"$outfile" 2>&1
      expected_phrase="validate-only stop"
    else
      make sync-athena-write-plan OWNER_USER_KEY=demo-owner OWNER_USER_ID=1 START_DATE="$start" END_DATE="$end" >"$outfile" 2>&1
      expected_phrase="write disabled"
    fi
    status=$?
    actual_write=$(awk '/^actual_write_enabled:/{print $2}' "$outfile")
    metrics_ok=$(awk 'BEGIN{flag=0;bad=0} /^pre_insert_metrics_compare:/{flag=1;next} /^pre_insert_gate_summary:/{flag=0} flag && $1 ~ /^[0-9][0-9][0-9][0-9]-/ && $NF != "false" {bad=1} END{print bad?"no":"yes"}' "$outfile")
    gate_ok=$(awk 'BEGIN{flag=0;bad=0} /^pre_insert_gate_summary:/{flag=1;next} /^candidate_row_sample:/{flag=0} flag && $1 ~ /^[0-9][0-9][0-9][0-9]-/ && $NF != "false" {bad=1} END{print bad?"no":"yes"}' "$outfile")
    tx_count=$(grep -c "$expected_phrase" "$outfile" | tr -d ' ')
    if [[ $status -eq 0 && "$actual_write" == "false" && "$metrics_ok" == "yes" && "$gate_ok" == "yes" && "$tx_count" -ge 1 ]]; then
      result="PASS"
    else
      result="FAIL"
    fi
    echo "$mode|$start|$end|status=$status|actual_write_enabled=$actual_write|metrics_ok=$metrics_ok|gate_ok=$gate_ok|tx_hits=$tx_count|result=$result"
  done
done

read-only sweep 沒跑起來，原因是 zsh 的 status 是保留變數，腳本裡撞名了。這不是產品邏輯問題；我只修終端腳本變數名稱，重新跑同一組 regression，不動 Go。

Ran terminal command:  cd /Users/pork/sofone-project/IA-Analyses/ia-analyses-db && tmpdir=$(mktemp -d) && windows=("w0101|2025-01-01|2025-01-01" "w0102|2025-01-02|2025-01-02" "w0101_0102|2025-01-01|2025-01-02" "w0107|2025-01-07|2025-01-07" "w0115|2025-01-15|2025-01-15" "w0131|2025-01-31|2025-01-31" "w0131_0201|2025-01-31|2025-02-01") && echo "tmpdir=$tmpdir" && for entry in "${windows[@]}"; do
  label=${entry%%|*}
  rest=${entry#*|}
  start=${rest%%|*}
  end=${rest#*|}
  for mode in validate writeplan; do
    outfile="$tmpdir/${label}_${mode}.txt"
    if [[ "$mode" == "validate" ]]; then
      make sync-athena-validate OWNER_USER_KEY=demo-owner OWNER_USER_ID=1 START_DATE="$start" END_DATE="$end" >"$outfile" 2>&1
      expected_phrase="validate-only stop"
    else
      make sync-athena-write-plan OWNER_USER_KEY=demo-owner OWNER_USER_ID=1 START_DATE="$start" END_DATE="$end" >"$outfile" 2>&1
      expected_phrase="write disabled"
    fi
    cmd_exit=$?
    actual_write=$(awk '/^actual_write_enabled:/{print $2}' "$outfile")
    metrics_ok=$(awk 'BEGIN{flag=0;bad=0} /^pre_insert_metrics_compare:/{flag=1;next} /^pre_insert_gate_summary:/{flag=0} flag && $1 ~ /^[0-9][0-9][0-9][0-9]-/ && $NF != "false" {bad=1} END{print bad?"no":"yes"}' "$outfile")
    gate_ok=$(awk 'BEGIN{flag=0;bad=0} /^pre_insert_gate_summary:/{flag=1;next} /^candidate_row_sample:/{flag=0} flag && $1 ~ /^[0-9][0-9][0-9][0-9]-/ && $NF != "false" {bad=1} END{print bad?"no":"yes"}' "$outfile")
    tx_count=$(grep -c "$expected_phrase" "$outfile" | tr -d ' ')
    if [[ $cmd_exit -eq 0 && "$actual_write" == "false" && "$metrics_ok" == "yes" && "$gate_ok" == "yes" && "$tx_count" -ge 1 ]]; then
      result="PASS"
    else
      result="FAIL"
    fi
    echo "$mode|$start|$end|exit=$cmd_exit|actual_write_enabled=$actual_write|metrics_ok=$metrics_ok|gate_ok=$gate_ok|tx_hits=$tx_count|result=$result"
  done
done

read-only regression 摘要出現 4 個窗口失敗，但目前看起來更像是「窗口沒有 candidate rows 時，摘要腳本把空窗口當失敗」，不一定是產品 bug。我先只讀失敗樣本輸出本身，確認是空窗口、gate 失敗，還是真的 source/candidate mismatch，再做分類；本輪不會先改 Go。Compacted conversation

Read [](file:///var/folders/kb/ywct5qs11lb10cwm771rc8j80000gp/T/tmp.iaxVtaXUSt/w0107_validate.txt)

Read [](file:///var/folders/kb/ywct5qs11lb10cwm771rc8j80000gp/T/tmp.iaxVtaXUSt/w0115_validate.txt)

Read [](file:///var/folders/kb/ywct5qs11lb10cwm771rc8j80000gp/T/tmp.iaxVtaXUSt/w0131_validate.txt)

Read [](file:///var/folders/kb/ywct5qs11lb10cwm771rc8j80000gp/T/tmp.iaxVtaXUSt/w0131_0201_validate.txt)

read-only matrix 已經有足夠訊號：1/1、1/2、1/1~1/2 通過；1/7、1/15、1/31、1/31~2/1 是 dimension gate 擋下，不是 compare 或 schema 問題。現在只跑你允許的 3 個 actual write window，並且在每次寫入前後都直接查 PG count 與 updated_at，確認沒有累加且時間戳有刷新。

Ran terminal command:  cd /Users/pork/sofone-project/IA-Analyses/ia-analyses-db && tmpdir=$(mktemp -d) && windows=("w0101|2025-01-01|2025-01-01" "w0102|2025-01-02|2025-01-02" "w0101_0102|2025-01-01|2025-01-02") && echo "tmpdir=$tmpdir" && for entry in "${windows[@]}"; do
  label=${entry%%|*}
  rest=${entry#*|}
  start=${rest%%|*}
  end=${rest#*|}
  before_file="$tmpdir/${label}_before.txt"
  after_file="$tmpdir/${label}_after.txt"
  out_file="$tmpdir/${label}_write.txt"
  candidate_file="$tmpdir/${label}_candidate.txt"
  docker compose exec -T postgres psql -U ia_admin -d ia_analyses -At -F '|' -c "SELECT business_date, COUNT(*), MIN(updated_at), MAX(updated_at) FROM public.pos_sales_hourly_fact WHERE owner_user_id = 1 AND business_date BETWEEN DATE '$start' AND DATE '$end' GROUP BY business_date ORDER BY business_date;" >"$before_file"
  make sync-athena-write-local OWNER_USER_KEY=demo-owner OWNER_USER_ID=1 START_DATE="$start" END_DATE="$end" >"$out_file" 2>&1
  cmd_exit=$?
  docker compose exec -T postgres psql -U ia_admin -d ia_analyses -At -F '|' -c "SELECT business_date, COUNT(*), MIN(updated_at), MAX(updated_at) FROM public.pos_sales_hourly_fact WHERE owner_user_id = 1 AND business_date BETWEEN DATE '$start' AND DATE '$end' GROUP BY business_date ORDER BY business_date;" >"$after_file"
  awk 'BEGIN{flag=0} /^write_skeleton_day_plan:/{flag=1;next} /^actual_write_enabled:/{flag=0} flag && $1 ~ /^[0-9][0-9][0-9][0-9]-/ {print $1 "|" $3}' "$out_file" >"$candidate_file"
  actual_write=$(awk '/^actual_write_enabled:/{print $2}' "$out_file")
  gate_ok=$(awk 'BEGIN{flag=0;bad=0} /^pre_insert_gate_summary:/{flag=1;next} /^candidate_row_sample:/{flag=0} flag && $1 ~ /^[0-9][0-9][0-9][0-9]-/ && $NF != "false" {bad=1} END{print bad?"no":"yes"}' "$out_file")
  post_ok=$(awk 'BEGIN{flag=0;bad=0} /^post_insert_metrics_compare:/{flag=1;next} flag && $1 ~ /^[0-9][0-9][0-9][0-9]-/ && $NF != "false" {bad=1} END{print bad?"no":"yes"}' "$out_file")
  rowcount_match=$(awk -F'|' 'NR==FNR{candidate[$1]=$2; next} {result=(candidate[$1]==$2)?"yes":"no"; print $1 ":candidate=" candidate[$1] ",after=" $2 ",match=" result}' "$candidate_file" "$after_file" | paste -sd ';' -)
  count_stable=$(awk -F'|' 'NR==FNR{before[$1]=$2; next} {result=(before[$1]==$2)?"yes":"no"; print $1 ":before=" before[$1] ",after=" $2 ",stable=" result}' "$before_file" "$after_file" | paste -sd ';' -)
  updated_refresh=$(awk -F'|' 'NR==FNR{before[$1]=$4; next} {result=(before[$1]!="" && before[$1]!=$4)?"yes":"no"; print $1 ":before=" before[$1] ",after=" $4 ",refreshed=" result}' "$before_file" "$after_file" | paste -sd ';' -)
  echo "window|$start|$end|exit=$cmd_exit|actual_write_enabled=$actual_write|gate_ok=$gate_ok|post_insert_ok=$post_ok"
  echo "rowcount_match|$rowcount_match"
  echo "count_stable|$count_stable"
  echo "updated_at_refresh|$updated_refresh"
  echo "after_rows|$(paste -sd ';' "$after_file")"
done

actual write 只在既有開放範圍內重跑了 3 個窗口，結果是穩定的：count 沒累加、post-insert compare 全部為 0、updated_at 也有刷新。接下來只做文件同步，把 5.R 的結論、失敗分類與 5.6 暫緩寫進既有說明。

Read [](file:///Users/pork/sofone-project/IA-Analyses/ia-analyses-db/README.md#130-130), lines 130 to 240

Read [](file:///Users/pork/sofone-project/IA-Analyses/ia-analyses-db/%E6%96%87%E4%BB%B6/%E6%9E%B6%E6%A7%8B%E6%8C%87%E5%8D%97.md#80-80), lines 80 to 231

Read [](file:///Users/pork/sofone-project/IA-Analyses/ia-analyses-db/%E6%96%87%E4%BB%B6/fact_implementation_sequence.md#190-190), lines 190 to 290

Read [](file:///Users/pork/sofone-project/IA-Analyses/ia-analyses-db/%E6%96%87%E4%BB%B6/%E6%9B%B4%E6%96%B0%E7%B4%80%E9%8C%84.md)

文件更新會只做狀態同步，不改流程與程式碼。我要把 5.R 的實際結果寫清楚：哪些窗口通過、哪些被 dimension gate 擋下、為什麼 5.6 必須繼續暫停。

Generating patch (120 lines) in [](file:///Users/pork/sofone-project/IA-Analyses/ia-analyses-db/README.md), [](file:///Users/pork/sofone-project/IA-Analyses/ia-analyses-db/%E6%96%87%E4%BB%B6/%E6%9E%B6%E6%A7%8B%E6%8C%87%E5%8D%97.md), [](file:///Users/pork/sofone-project/IA-Analyses/ia-analyses-db/%E6%96%87%E4%BB%B6/fact_implementation_sequence.md), [](file:///Users/pork/sofone-project/IA-Analyses/ia-analyses-db/%E6%96%87%E4%BB%B6/%E6%9B%B4%E6%96%B0%E7%B4%80%E9%8C%84.md), [](file:///Users/pork/sofone-project/IA-Analyses/ia-analyses-db/%E6%96%87%E4%BB%B6/sales_fact_regression_windows_phase2c.md)


```

```

```

```

```

```

```

```

```

```
文件已經同步完 5.R 的結果與停損邊界。我再跑一次 build 當作收尾驗證，並把剛才 zsh 的只讀變數踩坑記進工具記憶，避免之後在同樣的終端腳本上再浪費一次。

Ran terminal command:  go build ./...

Read memory [](file:///Users/pork/Library/Application%20Support/Code/User/globalStorage/github.copilot-chat/memory-tool/memories/tooling.md)

Inserted into memory file [](file:///Users/pork/Library/Application%20Support/Code/User/globalStorage/github.copilot-chat/memory-tool/memories/tooling.md)

結果已經齊了。最後我只抓一下文件中的實際落點行號，方便你直接跳到 5.R 的 correctness basis、regression 結果與更新紀錄。

Searched for text `Phase 2C-5.R` (`**/IA-Analyses/ia-analyses-db/README.md`), 2 results

Searched for text `Phase 2C-5.R` (`**/IA-Analyses/ia-analyses-db/文件/架構指南.md`), 1 result

Searched for text `Phase 2C-5.R` (`**/IA-Analyses/ia-analyses-db/文件/fact_implementation_sequence.md`), 1 result

Searched for text `Phase 2C-5.R` (`**/IA-Analyses/ia-analyses-db/文件/更新紀錄.md`), 1 result

Searched for regex `# Sales Fact Correctness Basis Phase 2C|# Sales Fact Regression Windows Phase 2C|## Observed Results` (`**/IA-Analyses/ia-analyses-db/文件/*.md`), 3 results

已完成 5.R 的文件與 regression 驗證。correctness basis 已定稿在 文件/sales_fact_correctness_basis_phase2c.md，regression window plan 與實測結果整理在 文件/sales_fact_regression_windows_phase2c.md。既有說明也已同步更新到 README.md、文件/架構指南.md、文件/fact_implementation_sequence.md、文件/更新紀錄.md。

驗證結果如下。
- go build ./... 通過。
- validate-only 與 write-plan 在 2025-01-01、2025-01-02、2025-01-01 ~ 2025-01-02 都通過，actual_write_enabled = false，source / candidate delta = 0。
- 2025-01-07、2025-01-15、2025-01-31、2025-01-31 ~ 2025-02-01 都不是 source / candidate mismatch，也不是 forbidden schema 問題；它們是在 pre-insert dimension gate 被擋下，分類為 dimension_bootstrap_issue。
- 具體 miss count 為：2025-01-07 product = 24、branch = 162；2025-01-15 product = 19、branch = 138；2025-01-31 product = 31、branch = 989。2025-01-31 ~ 2025-02-01 也在 2025-01-31 就先停下，沒有繼續到 2025-02-01。
- actual write 只重跑了允許範圍：2025-01-01、2025-01-02、2025-01-01 ~ 2025-01-02。結果都穩定：2025-01-01 維持 103545 筆，2025-01-02 維持 98167 筆，沒有累加，post-insert delta = 0，而且 updated_at 每次 rerun 都有刷新。

這一輪沒有改 Go 邏輯，只補文件並完成 regression sweep。5.6 的 7-day local write validation 目前維持暫停，先等 5.R 失敗窗口的 dimension bootstrap disposition 做完，再往下走。

下一步如果你要我接著做，我建議只有兩個方向：
1. 直接針對 2025-01-07、2025-01-15、2025-01-31 做 dimension gap root cause，確認缺的是 product dim、branch dim，還是 bootstrap window 不足。
2. 先把 5.R 的失敗窗口整理成可執行的 dimension backfill plan，再決定是否重新跑這幾個 read-only window。

已進行變更。