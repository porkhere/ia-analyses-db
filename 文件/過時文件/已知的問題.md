時間
2026/06/02 00:00:00

問題摘要
ia_users 設計尚未完成：owner_user_key 與 Athena database 的對應關係未正式定義。目前 seed 使用佔位值 demo-owner（id=1），實際業務應使用 50lan_new 或對應的租戶識別碼。未來若接多租戶，ia_users 的 key 命名規則與寫入流程需要正式設計。此設計變更會同時影響 ia-analyses-go 的 owner_user_key 參數傳遞方式與所有既有落地資料。

--------------------

時間
2026/05/26 17:11:33

問題摘要
Go pipeline 的主要操作入口已轉移到 `ia-analyses-go`，但本 repo 仍保留 bridge copy 的 `cmd/` 與 `internal/`，短期內存在雙份 Go slice 需要人工辨識主責邊界