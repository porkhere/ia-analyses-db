時間：2026/06/16 17:00:00

工作摘要：
建立 POC 目標文件

工作結果：
- 新增 `文件/POC目標.md`
- 明確 DB 在 POC 中只被 `ia-analyses-go` 接觸
- 明確 POC 第一版不為 MCP 或材料 BOM 新增 schema
- 明確 tenant / owner mapping 是 POC 前需要收斂的資料模型決策

待辦事項：
- 將 `demo-owner` 收斂為正式 POC tenant key 或明確 dev seed

--------------------

時間：2026/06/16 16:00:00

工作摘要：
code review 與文件校準 checkpoint

工作結果：
- 新增 `文件/code-review 改善事項.md`，整理 owner/tenant、bridge copy、init/patch 漂移、smoke analytics 與 branch group 缺口
- 校準 README 中 bridge copy 描述，移除已清除的 `sync-athena-*` target 語氣
- 確認基本文件仍以 `已知的問題`、`當前工作摘要`、`架構指南`、`table 結構文件`、`code-review 改善事項` 為主

待辦事項：
- 正式前端 MVP 前仍需決定 owner key / tenant mapping
- Go bridge copy 暫可保留，但新增 pipeline 功能應只進 `ia-analyses-go`

--------------------

時間：2026/06/22 11:10:00

工作摘要：補齊 DB smoke analytics 相關檢查並記錄狀態。

工作結果：
- 在 `scripts/db_smoke_analytics.sh` 新增 product-summary grain 的 minimal aggregation query（count 與 top5 preview），已通過 `bash -n` 語法檢查並提交至 repo。
- 2026/06/22 已在本機執行 runtime smoke，發現 preview SQL 的欄位模糊參考錯誤；已修正 script（qualify columns），再次執行後 preview 與所有既有檢查均回傳預期結果（含 top5 preview）。

補充：`pos_branch_dim.group_code` 欄位已在 schema 中定義，但目前同步流程不會填入該欄位；因此 `group_code` 在 POC 階段不作為前端 filter 的 authoritative source。若要啟用 branch-group filter，請先定義 authoritative source 並更新 sync pipeline 與 patch 文件。

待辦事項：
- 在能連到容器的環境執行 `make dev-smoke-analytics`，確認 preview 結果並更新本檔與 `code-review 改善事項.md`。


時間：2026/06/05 14:00:00

工作摘要：
修正 `.gitignore`：補齊 Athena/DB sync 資料保護、重型資料副檔名保護

工作結果：
- 補齊 `data/`、`exports/`、`tmp/`、`athena-output/`、`sync-output/` 目錄保護
- 補齊 `*.parquet`、`*.csv`、`*.csv.gz`、`*.gz`、`*.dump`、`*.backup`、`*.db`、`*.sqlite`、`*.sqlite3`、`*.duckdb` 副檔名保護
- 原有 `backup/**/*.dump` 規則與 `sync-athena` 根錨定保留不動
- `reports/` 與 `state/` 中已追蹤的文件不受影響（均為既有 tracked 文件）
- `git check-ignore` 驗證全部通過

待辦事項：
無

--------------------

時間
2026/06/01 00:00:03

工作摘要
新增 db-psql 維護工具 target

工作結果
在維護工具群組新增 db-psql，進入目前 .env 所指向的資料庫 psql shell，不需要區分 dev/prod

待辦事項
無

--------------------

時間
2026/06/01 00:00:02

工作摘要
重構 Makefile：清除殭屍 target，整理群組結構

工作結果
- 移除 sync-athena-* 殭屍 target（6 個）
- 移除 db-* 退役 stub（11 個）
- 過渡期 Go target 改歸「開發測試用」群組
- 架構指南對應段落同步更新

待辦事項
- 評估「開發測試用」群組的 sales-pipe-* / sync-sales-dims* 是否需要移到 ia-analyses-go

--------------------

時間
2026/06/01 00:00:01

工作摘要
依新規章補建架構指南速覽區塊

工作結果
在架構指南最上方新增速覽區塊（定位、主要入口、依賴、類型）

待辦事項
無

--------------------

時間
2026/06/01 00:00:00

工作摘要
refresh ws

工作結果
已完成基線文件更新，內容反映最新變動；在架構指南與 README 中標注 Makefile 殭屍 target（sync-athena-* 系列呼叫不存在的 cmd/sync-athena）

待辦事項
- 考慮清除或修正 Makefile 內 sync-athena-* 殭屍 target

--------------------

時間
2026/05/26 17:11:33

工作摘要
初始化工作，建立基線文件

工作結果
完成 DB repo 新版 README、架構指南、table 結構文件、工作摘要與已知問題文件建立，內容反映目前的 DB runtime、schema 與 bridge copy 現況

待辦事項
無

--------------------
