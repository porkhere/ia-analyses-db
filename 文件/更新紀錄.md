# 更新紀錄

最後檢視：2026-05-20

## 2026-05-20

- 新增 `文件/README.md`，建立 active / 過時文件索引，並將 phase2c、QuickSight 與未分類歷史文件移入 `文件/過時/` 的對應子資料夾
- 本輪只整理文件位置、索引與交叉引用；沒有改 Go code、沒有改 DB schema、沒有改 pipeline runtime、沒有改 Makefile 行為
- 建立 `backup/dev/baseline/manifest.md`，固定 baseline 名稱、建立時間、owner/date window 狀態、主要表 row count、smoke expectation 與注意事項；目前明確標示 repo 尚無 tracked baseline dump
- 新增 `make dev-restore-baseline` 與對應腳本；此入口只接受 `backup/dev/baseline/*.dump`，若缺少 tracked baseline dump 會明確失敗，不會假成功
- 新增 `make dev-smoke-analytics` 與對應腳本；此入口會檢查 `pos_product_dim`、`pos_branch_dim`、`pos_sales_hourly_fact` 是否非空，並執行帶有 `幣/券/折抵/折扣/點數/贈/服務費/運費/調整/測試/test` 排除規則的商品排行榜 smoke query；資料不足時會 `smoke failed` 並以 non-zero 結束
- 重新驗證本機 dev PostgreSQL 後確認 `pos_product_dim = 0`、`pos_branch_dim = 0`、`pos_sales_hourly_fact = 0`；因此本輪沒有提交 baseline dump、沒有假造資料，也沒有跑長時間 Athena / 一個月 pipeline
- README、架構指南與 minimal analytics baseline plan 已同步更新，將本輪狀態收斂為「先固定 baseline 入口與失敗語意，待有已驗證來源後再提交正式 baseline dump」

- 新增 [../reports/analytics_productization_gap_20260520.md](../reports/analytics_productization_gap_20260520.md)，盤點目前 analytics 已有能力、缺口與 product roadmap 起點
- 本輪明確確認目前 repo 已具備的 analytics 基礎包含：`pos_sales_hourly_fact`、`pos_product_dim`、`pos_branch_dim`、`pos_order_type_dim`、`pos_payment_type_dim`、`pos_order_status_dim`、`sales-pipe-*` controller、`sync-athena-*` / `sync-sales-dims*` pipeline，以及 `過時/quicksight/quicksight_metric_mapping.md`
- 本輪明確確認目前沒有 materialized view，也沒有 API server / API layer；目前可直接產品化的是 SQL 型排行榜、時段銷售、canonical payment / order type mix 與稅/折扣/附加費摘要
- 本輪結論是：analytics productization 的第一優先不再是擴 schema，而是補 `可重現 baseline data`、`商品語意分類`、`read-only API layer` 與後續 serving aggregation
- 飲料 POS analytics system 的高價值候選功能已先收斂成 10 個方向，包含商品排行、門店排行、時段銷售、category mix、payment/order type mix、異常偵測、低銷量商品監控、forecast v1 與 basket analysis v1

- 盤點目前 repo 是否已有「可重建非空 analytics baseline」機制：確認 `backup/dev/`、`backup/prod/` 目前都只有 `.gitkeep`，`db/backups/` 為空，repo 內沒有已提交的 baseline dump
- 直接檢查 `db/init/001_schema.sql` 與 `scripts/db_sync_seeds.sh`，確認現有 seed 只涵蓋 schema 與 canonical mapping / status seed，不會產生非空 `pos_product_dim`、`pos_branch_dim`、`pos_sales_hourly_fact`
- 直接檢查 `scripts/db_backup.sh`、`scripts/db_restore.sh`、`scripts/db_del_backup.sh`，確認目前通用 backup rotation 與 restore 都只掃 `backup/$APP_ENV` 第一層 `*.dump`；若未來要提交 tracked analytics baseline，應放在 `backup/dev/baseline/` 子目錄以避免被輪替或清除
- 本輪新增 [minimal_analytics_baseline_plan.md](minimal_analytics_baseline_plan.md)，完成「可重現最小 analytics baseline」方案評估、流程草圖、Makefile 建議入口與治理方式
- 本輪結論：目前 POC 最適合採 `hybrid`，也就是用已提交的 tracked minimal backup dump 作為 restore 載體，再配 manifest 與 smoke validation；其中單一載體的核心仍是 `backup 入 git`
- 下一輪最小實作範圍已固定為：建立一份小日期窗的 tracked dev baseline dump、加入 manifest、補 `make dev-restore-baseline` 與 `make dev-smoke-analytics`

- 建立 dev 重新開始基準：`git status --short` 無輸出、`git rev-list --left-right --count HEAD...@{upstream}` = `0 0`
- `make dev-env`、`make dev-up`、`docker compose ps`、`make dev-migrate`、`make dev-size` 均通過；PostgreSQL 容器健康，database size 約 `7.84 MB`
- 直接查 PostgreSQL 確認核心表皆存在，但當前 dev 只有 seed 維度有資料：`ia_users = 0`、`pos_product_dim = 0`、`pos_branch_dim = 0`、`pos_sales_hourly_fact = 0`、`pos_order_type_dim = 10`、`pos_payment_type_dim = 8`、`pos_order_status_dim = 4`
- 本輪明確確認 `make sales-pipe-status` 讀的是 [../state/sales_fact_pipe_state.json](../state/sales_fact_pipe_state.json) 的 controller state，不足以證明目前 dev PostgreSQL 已載入歷史 31 天資料
- 本輪商品語意調查確認：`pos_product_dim` 只有 `product_no`、`product_name`、`product_name_normalized`、`cate_no`、`cate_name` 等欄位，沒有 `normal_sales_item` / `product_semantic_type` 類的直接判斷欄位
- 直接查本機 PG 的「雲林幣」與名稱關鍵字疑似非商品項皆為 `0 rows`；原因是當前 `pos_product_dim` 與 `pos_sales_hourly_fact` 為空表，不代表來源一定不存在
- 嘗試用 Athena 補來源實證時，因缺少可用 AWS shared config / credential 而失敗；`make sync-sales-dims-plan ...` 報錯 `failed to get shared config profile, default`
- 新增本輪短報告 [../reports/phase2c_restart_baseline_20260520.md](../reports/phase2c_restart_baseline_20260520.md)，結論是下一輪應先補商品語意分類 / exclusion rule，再做一個月資料分析

## 2026-05-19

- Makefile 正式入口改為 `dev-*` / `prod-*`，補齊 `dev-env`、`prod-env`、`dev-up`、`prod-up`、`dev-migrate`、`prod-migrate`、`dev-backup`、`prod-backup`、`dev-restore`、`prod-restore` 等憲法要求指令
- 舊 `db-*` 命名全面退役；保留失敗提示，避免繼續誤用過時介面
- 新增 `.env.dev`、`.env.prod`，並在 `.env.example` 補上 `APP_ENV=dev`，讓 `.env` 明確成為目前工作環境檔
- backup 路徑從 `db/backups/` 改為 `backup/dev/` 與 `backup/prod/`，備份檔名收斂為 `YYYY-MM-DD-HH-MM.dump`，每環境最多保留 5 份
- restore 流程改成先列出備份、支援數字選擇與 `n` 退出；執行前自動做 pre-restore backup，完成後補跑基本 PostgreSQL 驗證
- 補齊 `文件/table 結構文件.md`，並同步更新 README、架構指南與根 repo 治理檔

## 2026-05-18

- 完成 Phase 2C-5.X-resume：修正 controller `write-plan` / `resume` 語義，`write-plan` 不再把 `planned` 汙染主 state，`resume` 會合併顯式 owner/date range，並以 state + PostgreSQL persisted dates 判定真正已完成日期
- resume fix 後重新執行 `go build ./...` 已通過
- 31-day resume 前檢查：`make sales-pipe-status` 與 PostgreSQL 查詢已確認 2025-01-01 ~ 2025-01-11 共 `1217900` 筆，`active_pipeline_process = false`，active `pg_stat_activity` query count = `0`
- `make sales-pipe-plan OWNER_USER_KEY=demo-owner OWNER_USER_ID=1 START_DATE=2025-01-01 END_DATE=2025-01-31` 已正確顯示 11 天 `completed`、20 天 `pending`，且不再覆寫主 execution state
- `make sales-pipe-resume OWNER_USER_KEY=demo-owner OWNER_USER_ID=1 START_DATE=2025-01-01 END_DATE=2025-01-31 CONFIRM_LONG_RUN=1` 已在 `32m51s` 內完成；controller 自動略過 2025-01-01 ~ 2025-01-11，成功寫入 2025-01-12 ~ 2025-01-31 共 20 天
- 31-day resume 完成後：`public.pos_sales_hourly_fact` 在 2025-01-01 ~ 2025-01-31 共 `3698110` 筆，`post_insert_delta_all_zero = true`，`product_dim_miss_total = 0`，`branch_dim_miss_total = 0`，`forbidden_column_count = 0`，`hard_gate_failed_count = 0`
- 完成後 `sales-pipe-status` 顯示 `active_pipeline_process = false`；table total size = `1029 MB`、table size = `590 MB`、indexes size = `439 MB`、database size = `1037 MB`
- 本輪 31-day summary report 為 [../reports/phase2c_sales_fact_pipe_summary_20260518T073547Z.md](../reports/phase2c_sales_fact_pipe_summary_20260518T073547Z.md)，檔案大小 `2.5 KiB`，仍不保存逐筆 candidate / insert row log

- 開始 Phase 2C-5.X-control：建立 Go execution controller，將長日期窗 orchestration 從 shell / Makefile 黑箱移到 [../internal/salespipe/controller.go](../internal/salespipe/controller.go)
- 新增 [../cmd/sales-pipe/main.go](../cmd/sales-pipe/main.go)，提供 `status`、`write-plan`、`validate-only`、`write-local`、`resume`、`report` 六種 controller mode
- Makefile 已新增 `sales-pipe-status`、`sales-pipe-plan`、`sales-pipe-validate`、`sales-pipe-write-local`、`sales-pipe-resume`、`sales-pipe-report`；Makefile 在 5.X-control 中只保留 thin entrypoint，不再承擔複雜任務編排
- controller state 固定落在 [../state/sales_fact_pipe_state.json](../state/sales_fact_pipe_state.json)，會記錄 run_id、date range、mode、status、current_business_date、completed_dates、failed_date、rollback_executed、total_rows_written_so_far 與 last_error_summary，不保存逐筆資料
- controller summary report 固定落在 `reports/phase2c_sales_fact_pipe_summary_<run_id>.md`；報告只保留耗時、每日 row count、validation summary、table size / DB size、report size 與 failure summary，不保存逐筆 candidate / insert row log
- 新增 [../reports/phase2c_31day_execution_incident_report.md](../reports/phase2c_31day_execution_incident_report.md)，明確記錄 31-day long shell orchestration 未完成、1/1~1/10 total_rows = `1086960`、incident snapshot database_size = `357 MB`、沒有證據證明 Athena / PostgreSQL / data model 壞掉，最可能問題是 orchestration / observability / interrupt handling
- `go build ./...` 已通過；`make help` 已列出新的 controller entrypoints；`make sales-pipe-status` 不跑 Athena、不寫 PG
- `make sales-pipe-plan OWNER_USER_KEY=demo-owner OWNER_USER_ID=1 START_DATE=2025-01-01 END_DATE=2025-01-31` 已通過，controller 能展開 31 天 day jobs / chunks 並寫入 state / summary report
- `make sales-pipe-validate OWNER_USER_KEY=demo-owner OWNER_USER_ID=1 START_DATE=2025-01-11 END_DATE=2025-01-11` 已通過；`source_candidate_delta_all_zero = true`，未寫 PostgreSQL
- `make sales-pipe-write-local OWNER_USER_KEY=demo-owner OWNER_USER_ID=1 START_DATE=2025-01-11 END_DATE=2025-01-11` 已通過；`row_count = 130940`、`post_insert_delta_all_zero = true`、`product_dim_miss_count = 0`、`branch_dim_miss_count = 0`、`forbidden_column_count = 0`
- smoke write 後 `sales-pipe-status` 與 `sales-pipe-report` 都可用；`active_pipeline_process = false`，summary report 可由 state 重建，不需重跑 Athena
- 本輪沒有修改 sales fact schema，也沒有修改 validation gate；31-day actual write 已改由 controller-based resume 完成，不再回到 31-day shell 長命令

## 2026-05-15

- 執行 Phase 2C-5.R：small-window regression validation；先新增 [過時/phase2c/sales_fact_correctness_basis_phase2c.md](過時/phase2c/sales_fact_correctness_basis_phase2c.md) 與 [過時/phase2c/sales_fact_regression_windows_phase2c.md](過時/phase2c/sales_fact_regression_windows_phase2c.md)，固定 correctness basis、窗口矩陣與 fail classification
- 5.R correctness basis 已明確化：Athena raw POS tables 是 source of record，但 row-level correctness 不是直接相信 raw rows，而是依據 status-aware semantic contract、source -> candidate compare、dimension gate、negative schema gate 與 candidate -> post-insert target compare；QuickSight 是 business benchmark，不是唯一 row-level truth
- 5.R read-only regression 結果：2025-01-01、2025-01-02、2025-01-01 ~ 2025-01-02 的 `sync-athena-validate` / `sync-athena-write-plan` 全部通過，`actual_write_enabled = false`，source / candidate metrics delta = `0`
- 5.R read-only regression 也驗證出新的 dimension gate failure window：2025-01-07 `product_dim_miss_count = 24`、`branch_dim_miss_count = 162`；2025-01-15 `product_dim_miss_count = 19`、`branch_dim_miss_count = 138`；2025-01-31 `product_dim_miss_count = 31`、`branch_dim_miss_count = 989`
- 2025-01-31 ~ 2025-02-01 的 read-only regression 在 2025-01-31 就先被同一組 dimension gate 擋下，未繼續到 2025-02-01；這一批失敗目前先分類為 `dimension_bootstrap_issue`，不是 `source_candidate_mismatch`、`post_insert_mismatch`、`validation_gate_issue` 以外的 `actual_code_bug`
- 5.R actual write regression 只重跑既有允許範圍：2025-01-01、2025-01-02、2025-01-01 ~ 2025-01-02；直接查詢 PostgreSQL 驗證 row count 仍維持 `103545` / `98167`，沒有累加，post-insert target metrics delta = `0`，`updated_at` 皆有刷新
- `go build ./...` 在 5.R 開始前重新通過；本輪沒有修改 Go 邏輯，只補文件與執行 regression validation
- README、架構指南、fact implementation sequence、更新紀錄 已同步改成「本輪暫停 7-day write 推進；只有在 5.R 全窗口通過並完成 failure disposition 後，才回到 5.6 controlled 7-day local write validation」

- 完成 Phase 2C-5.5：controlled 2-day local actual write，將 `sync-athena-write-local` 從單日放寬成最多 2 天的小範圍驗證
- 2025-01-01 ~ 2025-01-02 實測：2025-01-01 寫入 `103545` 筆、2025-01-02 寫入 `98167` 筆；兩天 transaction result 都是 `commit`
- 2025-01-01 ~ 2025-01-02 實測：兩天 post-insert target metrics delta 都為 `0`，兩天 `product_dim_miss_count = 0`、`branch_dim_miss_count = 0`、`forbidden_column_count = 0`
- 直接查詢 PostgreSQL 驗證：`pos_sales_hourly_fact` 兩天 row count 分別為 `2025-01-01 = 103545`、`2025-01-02 = 98167`，與各日 candidate row count 一致
- `sync-athena-write-local` 在 5.5 仍採逐日 day-level transaction；若任一天 fail，該日 rollback，且不得繼續後一天
- `sync-athena-validate` 與 `sync-athena-write-plan` 在 5.5 重新驗證後仍是 read-only，`actual_write_enabled = false`，不會 delete / insert / commit
- 超過 2 天的 `sync-athena-write-local START_DATE=2025-01-01 END_DATE=2025-01-03` 在 5.5 仍直接拒絕
- README、架構指南、fact implementation sequence、更新紀錄 已同步補上 5.5 controlled 2-day local actual write 的邊界與驗證結果；下一步改為 7-day local write validation，不是 31 天，也不是 production

- 完成 Phase 2C-5.4：sales fact local write idempotency validation，確認 2025-01-01 的單日 `sync-athena-write-local` 可以安全 rerun
- 直接查詢 PostgreSQL 驗證：rerun 前 `pos_sales_hourly_fact` row count = `103545`、rerun 後 row count = `103545`，沒有累加成 `207090`
- 直接查詢 PostgreSQL 驗證：`updated_at` 由 `2026-05-15 17:54:58.779713+08` 刷新為 `2026-05-15 17:56:46.074969+08`，證明 rerun 真的重做 day-level replace
- rerun 後 `sync-athena-write-local` 仍顯示 `target_row_count = 103545`、post-insert target metrics delta = `0`、transaction result = `commit`
- rerun 後重新執行 `sync-athena-validate`，`product_dim_miss_count = 0`、`branch_dim_miss_count = 0`、`forbidden_column_count = 0`，read-only path 未受污染
- `sync-athena-write-plan` 在 5.4 重新驗證後仍是 read-only，`actual_write_enabled = false`，不會 delete / insert / commit
- 多日 `sync-athena-write-local START_DATE=2025-01-01 END_DATE=2025-01-02` 在 5.4 仍直接拒絕，不開放多日 actual write
- README、架構指南、fact implementation sequence、更新紀錄 已同步補上 5.4 idempotent rerun validation 結論；下一步改為 controlled 2-day local actual write

- 完成 Phase 2C-5.3：開啟 local-only sales fact actual write，新增 `sync-athena-write-local`；本輪只允許 `public.pos_sales_hourly_fact` 的單日 day-level replace
- `sync-athena-write-local` 必須顯式使用 `--write-pg` + `--local-only-actual-write`，且要求 local PostgreSQL target 與 `START_DATE = END_DATE`；多日輸入會直接拒絕
- `sync-athena-validate` 與 `sync-athena-write-plan` 在 2C-5.3 仍保持 read-only，不寫 PostgreSQL；`db-sync-athena` 仍是安全停止入口
- `SQLSalesFactWriter` 已接上 local-only actual write；實際流程固定為 transaction 內 delete + insert + post-insert compare，hard gate fail 或 compare fail 都會 rollback
- 2025-01-01 實測：`sync-athena-write-local` 寫入 `pos_sales_hourly_fact = 103545` 筆，post-insert target metrics 與 candidate metrics delta = `0`，transaction result = `commit`
- 寫後重新執行 `sync-athena-validate`，`product_dim_miss_count = 0`、`branch_dim_miss_count = 0`、source / candidate metrics delta = `0`、`forbidden_column_count = 0`
- `item_count` 仍只允許 validation-only；raw payment、void / refund / order-level metrics 仍未進 persisted sales fact
- `branch conflict count = 6` 目前仍視為 dimension warning，不阻擋 sales fact write，但後續需追查 branch 名稱來源一致性
- README、架構指南、fact implementation sequence、更新紀錄 已同步補上 2C-5.3 的單日 local-write 邊界、read-only 入口不變與下一步多日 / idempotent rerun 驗證

- 完成 Phase 2C-5.2：新增 `sync-sales-dims-plan` 與 `sync-sales-dims`，提供 sales fact dimension bootstrap / sync 路徑；本輪只處理 `pos_product_dim` / `pos_branch_dim`，不處理任何 sales fact actual write
- `sync-sales-dims-plan` 會真的查 Athena，輸出 `pos_product_dim` / `pos_branch_dim` 的 planned upsert count 與 conflict summary；`sync-sales-dims` 才允許 upsert 這兩張 dim 表
- 2025-01-01 ~ 2025-01-02 驗證結果：planned upsert `pos_product_dim = 487`、`pos_branch_dim = 277`，product conflict = `0`、branch conflict = `6`
- `sync-sales-dims` apply 已驗證只寫 `pos_product_dim` / `pos_branch_dim`；`written_tables = pos_product_dim, pos_branch_dim`，`sales_fact_written = false`
- dimension sync 後重新執行 `sync-athena-validate`，`product_dim_miss_count = 0`、`branch_dim_miss_count = 0`，source / pre-insert candidate metrics delta 仍為 `0`，`actual_write_enabled = false`
- branch name conflict selection 規則固定為「most frequent variant; tie -> latest seen; tie -> lexical」；`pos_branch_dim.group_code` 因來源尚未確認，本輪固定寫 `NULL`
- README、架構指南、fact implementation sequence、更新紀錄 已同步補上 2C-5.2 的安全邊界、Make target 與驗證結果；下一輪才進 local-only sales fact actual write / day-level replace

- 完成 Phase 2C-5.1：接上 Athena status-aware sales source candidate provider；`sync-athena-validate` / `sync-athena-write-plan` 現在都會真的查 Athena，輸出 source metrics、pre-insert candidate metrics、validation gate summary 與 candidate row sample
- 新增只讀 PostgreSQL validation reader，會讀取 product / branch / order_type / payment_type dim 與 negative schema gate；sample run 目前因 product / branch dim miss 停在 pre-insert hard gate，未進入任何 write
- `DisabledSalesFactWriter` 仍是 runtime writer；本輪未執行 PostgreSQL insert，但 post-insert target metrics reader 已可供後續 actual write 階段使用
- `internal/athena/plan.go`、README、架構指南、fact implementation sequence 已同步改成 Phase 2C-5.1 現況與下一步 actual write / post-insert compare 方向
- 確認 [../internal/postgres/write_skeleton.go](../internal/postgres/write_skeleton.go) 已存在，並保留 `DisabledSalesFactWriter` 與 `SQLSalesFactWriter` skeleton；本輪仍未開啟 actual write
- `db-sync-athena` 已改成安全停止入口，明確提示改用 `sync-athena-dry`、`sync-athena-validate`、`sync-athena-write-plan`，避免 Phase 2C-5 skeleton 期間誤用
- README、架構指南、fact implementation sequence、更新紀錄 已同步補上 `--write-pg`、`--validate-only`、`--owner-user-id`、`sync-athena-validate`、`sync-athena-write-plan` 的目前狀態，並明確寫出 `sync-athena-write-plan` 只輸出 day-level skeleton plan，不跑 Athena、不寫 PostgreSQL
- 下一輪最小工作已固定為接上 Phase 2B status-aware sales source candidate provider；在接通前仍須維持 disabled writer 或 validate-only
- 開始 Phase 2C-5 implementation：新增 sales fact PG write path skeleton 與 validation gate integration skeleton，但 actual production write 仍維持 disabled
- `sync-athena` 已新增 / 預留 `--write-pg` 與 `--validate-only` 安全模式；目前只會輸出 day-level write skeleton plan 或 validation gate plan，不會執行 Athena、也不會真正寫 PostgreSQL
- 新增 `internal/validation` gate skeleton、`internal/sales` day-level replace orchestration skeleton，以及 `internal/postgres` transaction / writer skeleton，固定 hard gate fail 必須 stop / rollback
- README、架構指南、fact implementation sequence 已同步標示：Phase 2C-5 已開始，但本輪只做 skeleton，不包含 order/payment/condiment/branch opening fact，也不允許 validation bypass
- 本輪未執行 Athena、未執行 PostgreSQL insert、未新增 migration、未修改 `db/init/001_schema.sql`、未修改 `db/patches`
- Phase 2C-4.7 文件同步補修：final review 已完成，確認文件為 [過時/phase2c/sales_fact_validation_final_review_phase2c.md](過時/phase2c/sales_fact_validation_final_review_phase2c.md)
- Phase 2C-5 可以開始，但只限 sales fact PG write path skeleton + validation gate 整合；仍禁止 validation bypass，hard gate fail 必須 stop / rollback
- 本輪補齊 README 與更新紀錄，明確寫回 2C-5 仍禁止擴張到 order fact、payment fact、condiment fact、branch opening fact，也不得把 raw payment、void / refund / order-level metrics、`item_count` 寫進 persisted sales fact
- 新增 [過時/phase2c/sales_fact_validation_final_review_phase2c.md](過時/phase2c/sales_fact_validation_final_review_phase2c.md)，完成 Phase 2C-4.7 final review，整併 contract、metrics review 與 gate review，明確列出 5 份 validation SQL draft 的責任邊界、hard gate、warning gate、insert 前後檢查與 rollback / stop 條件
- README、架構指南、fact implementation sequence 已同步補上「Phase 2C-4.7 final review 已完成」與「Phase 2C-5 可以開始，但僅限 sales fact PG write path skeleton + validation gate 整合」
- README、架構指南、fact implementation sequence、更新紀錄 已再同步明確化：Phase 2C-5 仍然禁止 order fact、payment fact、condiment fact、branch opening fact，以及 raw payment、void / refund / order-level metrics、`item_count` 寫進 persisted sales fact，也不得繞過 validation gate
- README、架構指南、fact implementation sequence、更新紀錄 已再同步明確化：Phase 2C-5 必須遵守 day-level replace、transaction boundary、validation first、pre-insert compare、dimension gate、negative schema gate、post-insert compare，且 hard gate fail 必須 stop / rollback
- 本輪未新增 validation SQL、未修改 schema、未新增 migration、未改 Go、未改 `sync-athena`、未跑 Athena、未寫 PostgreSQL
- Phase 2C-4.6 小修正：將 `item_count` 加入 negative schema forbidden columns，並同步明確化「`item_count` 是 validation-only control metric，不得進 `pos_sales_hourly_fact` persisted schema」
- 新增 [sales_fact_dimension_gate_checks.sql](../db/validation_draft/sales_fact_dimension_gate_checks.sql) 與 [sales_fact_negative_schema_checks.sql](../db/validation_draft/sales_fact_negative_schema_checks.sql)，補齊 Phase 2C-4.6 的 validation gate SQL draft
- 新增 [過時/phase2c/sales_fact_validation_gate_review_phase2c.md](過時/phase2c/sales_fact_validation_gate_review_phase2c.md)，明確定義 Phase 2C-4.5 已完成 metrics reconciliation draft、Phase 2C-4.6 補齊 dimension / negative schema gate，且 Phase 2C-5 仍不得開始
- README、validation contract、validation SQL review 已同步補上 4.6 gate drafts 與「metrics review + gate review 都通過前不得進 2C-5」的條件
- 新增 [過時/phase2c/sales_fact_validation_contract_phase2c.md](過時/phase2c/sales_fact_validation_contract_phase2c.md)，定義 Phase 2C-4 的 sales fact 寫入前 validation contract 與 SQL 草案
- README、架構指南、fact implementation sequence 已同步改成：Phase 2C-3 本機開發 DB patch 驗證通過，Phase 2C-4 先凍結 validation contract，Phase 2C-5 才開始 sales fact PG write path
- 本輪未修改 `db/init/001_schema.sql`、未新增 migration、未修改 `db/patches`、未改 Go、未改 `sync-athena`、未跑 Athena、未寫 PostgreSQL
- Phase 2C-3-pre 小修正：將 `pos_order_status_dim.updated_at` 從 `timestamp without time zone` 改成 `TIMESTAMPTZ`，與 repo 既有 audit 欄位慣例一致
- README 的目前資料表清單已補上 `pos_product_dim.cate_no/cate_name`、`pos_branch_dim.group_code`，以及 `pos_order_status_dim`
- 新增正式 patch [../db/patches/003_phase2c_schema_contract.sql](../db/patches/003_phase2c_schema_contract.sql)，將 Phase 2C schema contract 轉成可套用的 additive migration
- 同步更新 [../db/init/001_schema.sql](../db/init/001_schema.sql)，讓新建 DB baseline 納入 `pos_order_status_dim`、product category、branch group code 與 `business_date = sale_period` comment
- README 與架構指南已補上 Phase 2C-3-pre formal migration 說明，明確標示本輪只建立 patch，不執行 migration、不寫 PostgreSQL
- 新增 [過時/phase2c/schema_migration_plan_phase2c.md](過時/phase2c/schema_migration_plan_phase2c.md)，將 Phase 2C-1 schema contract 轉成 additive schema migration plan 草案
- README、架構指南、fact implementation sequence 已同步補上 Phase 2C-2 migration plan 引用，明確改成「先審 plan，再建立 migration」
- Phase 2C-2 目前仍未修改 `db/init/001_schema.sql`、未新增 migration、未寫 PostgreSQL、未改 `sync-athena`
- 新增 QuickSight compatibility review 文件集，包含 analysis inventory、metric mapping、fact gap analysis、validation plan
- 正式收斂多 fact data model 設計，不再把 `pos_sales_hourly_fact` 定義成單一萬用 fact
- 新增 `文件/data_model_design.md`，定義 sales / order / payment / condiment / branch opening 五類 fact 邊界
- 新增 `過時/phase2c/fact_implementation_sequence.md`，將 Phase 2C 拆成語意凍結、sales fact contract、sales fact 寫入與後續 fact 階段
- 暫停直接往 PostgreSQL 寫入 skeleton 的方向，下一步改為先定稿 fact boundary 與日期語意
- README、架構指南、fact gap analysis、validation plan 全部補上多 fact 分層與 QuickSight compatibility 原則

## 2026-05-14

- 建立 `ia-analyses-db` 第一階段 repo 骨架
- 新增 PostgreSQL Docker Compose 與 `.env.example`
- 新增 `db/init/001_schema.sql`，建立六張核心資料表與 seed mapping
- 新增 `db_backup.sh`、`db_restore.sh`、`db_del_backup.sh`
- 新增 `sync-athena` Go CLI skeleton，支援參數驗證與 `--dry-run`
- 補上 README 與架構指南，明確標示 phase 1 僅為基礎設施與骨架
- 新增 `make db-size` 與 `make db-backup-list`，支援本機容量與備份檢查
- 擴充 `sync-athena --dry-run`，加入時間、筆數、預估體積與執行時間的 heuristics 輸出
- 補齊 `pos_order_type_dim` 與 `pos_payment_type_dim` seed 類型，並新增 `make db-sync-seeds` 供現有開發 DB 安全套用
- 補上 dry-run 估算模型的文件說明，明確標示目前不是實際 Athena 查詢結果
- 將 `make sync-athena-dry` 收斂成 fast mode，並新增 `make sync-athena-dry-full` 保留完整 debug 輸出
- `sync-athena --dry-run` 新增 `--dry-run-mode=fast|full`，fast mode 只保留必要查詢，full mode 保留 mapping、preview sample 與既有 debug 區塊
- 完成 status-aware preview sales path 的長日期窗 fast 驗證
- 7 天 fast 驗證結果：result_aggregation 775,736、tax delta -9,297、net delta 365、sales_ex_tax delta 9,662、top tax delta 最大絕對值 6、總耗時 64.17 秒
- 31 天 fast 驗證結果：result_aggregation 3,698,110、tax delta -45,109、net delta 1,511、sales_ex_tax delta 46,620、top tax delta 最大絕對值 7、總耗時 87.27 秒
- 文件同步補上 fast / full dry-run 的差異、使用時機，以及長日期窗驗證結論